name: "Setup Zig"
description: "Install Zig compiler with version input support."
author: "Jetsung Chan"
branding:
  icon: "zap"
  color: "blue"
inputs:
  version:
    description: "Zig version (e.g. 0.15.2, dev, master). Leave empty for latest stable."
    required: false
    default: ""
runs:
  using: "composite"
  steps:
    - name: "Setup Zig"
      shell: bash
      run: ${{ github.action_path }}/install.sh
      env:
        VERSION_INPUT: ${{ inputs.version }}